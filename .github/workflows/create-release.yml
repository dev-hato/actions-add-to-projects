---
name: create-release

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/create-release.yml
      - action.yml

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.1.0
      - uses: dev-hato/actions-create-release@v0.0.10
        with:
          github-token: ${{secrets.GITHUB_TOKEN}}
